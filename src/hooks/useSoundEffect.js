/**
 * useSoundEffect Hook Stub
 * For future wiring of ambient sounds (e.g. quill scratch, paper rustle, soft candle crackle, wax seal clink).
 * Usage:
 * const playSound = useSoundEffect();
 * playSound('click');
 */
export const useSoundEffect = () => {
  const playSound = (actionName) => {
    // Stub function — do nothing for now. Can log to console for debugging if needed:
    // console.log(`[Sound Effect Triggered]: ${actionName}`);
  };
  
  return playSound;
};
